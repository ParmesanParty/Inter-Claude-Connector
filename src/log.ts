// Minimal stub — log.ts was gutted when send_prompt / claude -p was removed.
// Tests import `reset` for isolation, so we keep no-op exports.

export function init(): void {}
export function reset(_newLogDir?: string): void {}
